@model QLHS.Models.cart

@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

@*<h2>Cart</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.book.book_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cart.id)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.book.book_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cart.id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }

    </table>
*@

<div class="breadcrumbs-area mb-70">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumbs-menu">
                    <ul>
                        <li><a href="#">Trang chủ</a></li>
                        <li><a href="#" class="active">giỏ hàng</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- breadcrumbs-area-end -->
<!-- entry-header-area-start -->
<div class="entry-header-area">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="entry-header-title">
                    <h2>Giỏ hàng</h2>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- entry-header-area-end -->
<!-- cart-main-area-start -->
<div class="cart-main-area mb-70">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <form action="#">
                    <div class="table-content table-responsive mb-15 border-1">
                        <table>
                            <thead>
                                <tr>
                                    <th class="product-thumbnail">Ảnh</th>
                                    <th class="product-name">Tên sách</th>
                                    <th class="product-price">Giá</th>
                                    <th class="product-quantity">Số lượng</th>
                                    <th class="product-subtotal">Tổng</th>
                                    <th class="product-remove">Xóa</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.cart_item)
                                {
                                    <tr>
                                        <td class="product-thumbnail"><a href="#"><img src="~/Assets/Upload/@item.book.image" alt="man" /></a></td>
                                        <td class="product-name"><a href="#">@Html.DisplayFor(modelItem => item.book.book_name)</a></td>
                                        <td class="product-price"><span class="amount">@Html.FormatValue(item.book.price_sale,"{0:#,###}") đ</span></td>
                                        <td class="product-quantity"><input class="ip" id="@item.id" type="number" min="1" value="@item.quantity"></td>
                                        <td class="product-subtotal">@Html.FormatValue(item.book.price_sale*item.quantity, "{0:#,###}") đ</td>
                                        <td class="product-remove"><a href="#" id="@item.id" class="delete-item"><i class="fa fa-times"></i></a></td>
                                    </tr>
            
                                }


                            </tbody>
                        </table>
                    </div>
                </form>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-8 col-md-6 col-12">
                <div class="buttons-cart mb-30">
                    <ul>
                        
                        <li><a href="#">Tiếp tục mua sắm</a></li>
                    </ul>
                </div>
               
            </div>
            <div class="col-lg-4 col-md-6 col-12">
                <div class="cart_totals">
                    <h2>Tổng Số Giỏ Hàng</h2>
                    <table>
                        <tbody>
                            <tr class="cart-subtotal">
                                <th>Tổng phụ</th>
                                <td>
                                    <span class="amount sumMoney">@Html.FormatValue(Model.total, "{0:#,###}") vnđ</span>
                                </td>
                            </tr>
                            <tr class="shipping">
                                <th>Giao hàng</th>
                                <td>
                                    <ul id="shipping_method">
                                        
                                        <li>
                                            <input type="radio" checked="checked">
                                            <label> Miễn phí vận chuyển </label>
                                        </li>
                                    </ul>
                                    <a href="#"></a>
                                </td>
                            </tr>
                            <tr class="order-total">
                                <th>Tổng cộng</th>
                                <td>
                                    <strong>
                                        <span class="amount sumMoney">@Html.FormatValue(Model.total, "{0:#,###}") vnđ</span>
                                    </strong>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                    <div class="wc-proceed-to-checkout">
                        <a href="@Url.Action("Checkout", "Home")">Thanh toán</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
